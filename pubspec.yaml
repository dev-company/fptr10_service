name: fptr10_service
description: unofficial ATOL plugin
version: 0.2.0
homepage: https://github.com/dev-company/fptr10_service

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  freezed: "1.1.0"
  freezed_annotation: "1.1.0"
  json_serializable: ^6.1.4
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner:

flutter:
  plugin:
    platforms:
      android:
        package: company.dev.fptr10_service
        pluginClass: Fptr10ServicePlugin
